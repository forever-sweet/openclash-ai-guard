# 更新记录

## v1.1.0

### 修复

**节点名里带空格时，坏节点统计全线误判**（影响所有人，建议升级）

看护脚本把 mihomo API 返回的 JSON 用 `sed`/`tr` 拆字符串，再用 `awk '$1==名字'`
查表。awk 默认按空白分字段，`$1` 只拿到名字的**第一个词** —— 于是任何名字里带
空格的节点都查不到延迟、被记成 `dead`。

中文机场的主流命名方式全中：`🇭🇰 香港 01`、`US 洛杉矶 02`、`HK-5 | 2x`。
实测 4 个节点里 3 个被误判：

```
'🇭🇰 香港 01'  -> DEAD ✗     'US 洛杉矶 02' -> DEAD ✗
'HK-5 | 2x'    -> DEAD ✗     'JP-1'         -> 100ms ✓
```

后果不只是日志数字不准：坏节点率会**常驻 100%**，于是连续 3 次过阈值 → 每过一次
4 小时冷却就去重拉一遍订阅，无限循环。名字里带逗号的还会被 `tr ','` 拆成两行，
`total` 都会算多。

现在改用 ruby 解析（JSON 是 YAML 子集，`ruby-yaml` 本来就是硬依赖，没引入新东西），
字段分隔符换成制表符。节点名可以含空格、逗号、冒号、emoji、竖线。

> 如果你的节点名是 `HK-1` `JP-2` 这种不带空格的，你不会受这个 bug 影响。

**`AI_DOCS_GROUP` 留空时会误删配置里的东西**

- `include?('')` 会匹配到任何**没有 `name` 字段**的策略组，把它删掉
- 幂等清理用的 `end_with?(",#{DOCS_GROUP}")` 会退化成 `end_with?(",")`，
  误删任何**以逗号结尾**的规则（比如 `DOMAIN-SUFFIX,example.com,`）

两个都已用新旧代码 A/B 实测确认。因为 AI-Docs 默认关闭，**所有用默认配置的人都
踩在这个坑上**。

**钩子追加失败时会谎报成功**

`hook-install.sh` 里 `awk ... && mv && chmod` 之后无条件 `echo` 成功消息，
awk 出错时也照样说"已追加"。现在失败会返回非零、并且校验新文件里真的有我们的
调用才覆盖，否则原钩子保持不动。

### 变更

- **仓库改名** `openclash-ai-guard` → `openclash-ai-proxy-group`。
  「策略组」是 OpenClash 界面里的原词，这样搜得到。
  **安装路径没变**（仍是 `/etc/openclash/ai-guard/`、`ai-guard.conf`、cron 标记
  `#ai-guard-watch`、日志 `/tmp/ai-guard.log`）—— 改这些会让老用户升级后装出
  两套并存的 cron 和钩子，所以故意不动。旧仓库地址 GitHub 会自动重定向。
- `install.sh` 删掉了 `HOOK_MANUAL` 分支 —— 这个变量从没被任何地方赋值过，
  而且它在子 shell 里也传不回来，是死代码。真实情况是 `hook-install.sh`
  三种情形全自动处理，不存在"要用户手动加两行"这条路。
- `--status` 现在显示版本号，启用了 AI-Docs 的话也会显示它当前的出口。
- 新增 `.gitignore`：防止把自己路由器上的真实配置（含你在用的中转站域名）
  提交进公开仓库。
- README 新增「我在用的机场」一节（含利益披露）。

## v1.0.0

首个版本。两个 AI 专用 `url-test` 出口组 + 分流规则注入 + 每 10 分钟看护脚本，
外加可选的 AI-Docs 免 WAF 验证组。
