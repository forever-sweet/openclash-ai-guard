# 保证 shell / ruby 脚本永远以 LF 结尾。
# Windows 上 clone 时如果被转成 CRLF，脚本传到路由器会直接
# 报 "bad interpreter: no such file or directory"。
* text=auto eol=lf
*.sh   text eol=lf
*.rb   text eol=lf
*.conf text eol=lf
*.md   text eol=lf
LICENSE text eol=lf
